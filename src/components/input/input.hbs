<input
    class="input {{classname}}"
    placeholder="{{placeholder}}"
    name="{{name}}"
    {{#if type}}
        type="{{type}}"
    {{else}}
        type="text"
    {{/if}}
    value="{{value}}"
/>
