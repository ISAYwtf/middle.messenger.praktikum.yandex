export interface InputProps {
    label: string,
    name: string,
    value?: string,
    type?: string,
    onBlur?: EventListener,
    onInput?: EventListener,
    placeholder?: string,
    classname?: string,
}
